model:
  name: Qwen/Qwen2.5-0.5B
  device: auto
  dtype: auto
  local_files_only: false

lora:
  rank: 4
  alpha: 8
  dropout: 0.0
  target_modules: [q_proj, v_proj]

training:
  learning_rate: 0.0005
  epochs: 1
  max_steps: null

